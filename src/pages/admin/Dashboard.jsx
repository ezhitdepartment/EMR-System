// Admin's Dashboard landing page. Intentionally empty for now — the
// content that goes here hasn't been decided yet. Ready to build out
// whenever there's a spec for what it should show.
export default function Dashboard() {
  return (
    <div className="max-w-7xl">
      <h1 className="text-2xl font-bold text-slate-800 mb-1">Dashboard</h1>
      <p className="text-sm text-slate-500">Nothing here yet — this page is ready for content.</p>
    </div>
  );
}
